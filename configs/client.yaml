server:
  address: "localhost:8080"
  tls_enabled: false
  timeout: 10

storage:
  path: "~/.gophkeeper/data.db"
  encryption_key: ""  # будет запрошен при первом запуске

log:
  level: "info"